services:
  - type: web
    name: unitasks-demo
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node server/app.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
