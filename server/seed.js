// Seed script - data is now loaded from seed-data.json at server startup
// This file is kept for reference. The server (app.js) handles seeding automatically.
console.log('Seed data is now loaded automatically from seed-data.json when the server starts.')
console.log('Run: node server/app.js  to start the demo server.')
